[package]
name = "blargg_runner"
version = "0.1.0"
license = "MIT"
edition = "2021"
description = "Test utility for Ceres emulator"

[dependencies]
ceres_core = { path = "../../emulator/ceres_core" }
crc = "*"

[profile.release]
opt-level = 3
lto = true
